diff .aliases.zsh ~/.aliases.zsh
ln -i .aliases.zsh ~/

diff .zshrc ~/.zshrc
ln -i .zshrc ~/

diff .tmux.conf ~/.tmux.conf
ln -i .tmux.conf ~/

diff .alacritty.yml ~/.alacritty.yml
ln -i .alacritty.yml ~/

diff .tool-versions ~/.tool-versions
ln -i .tool-versions ~/

diff .p10k.zsh ~/.p10k.zsh
ln -i .p10k.zsh ~/

diff .tmuxline_snapshot ~/.tmuxline_snapshot
ln -i .tmuxline_snapshot ~/

diff .config/nvim/init.vim ~/.config/nvim/init.vim
ln -i .config/nvim/init.vim ~/.config/nvim/

diff .config/nvim/coc-settings.json ~/.config/nvim/coc-settings.json
ln -i .config/nvim/coc-settings.json ~/.config/nvim/
